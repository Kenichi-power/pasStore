import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 30,
  },
  counter: {
    position: 'absolute',
    bottom: 10,
    right: 10,
  },
})
export default styles
